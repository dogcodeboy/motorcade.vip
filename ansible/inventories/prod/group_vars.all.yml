# Example non-secret inventory overrides
# Copy to: inventories/prod.example/group_vars/all.yml (or your prod inventory)

# ansible_user: ec2-user
# ansible_ssh_private_key_file: ~/.ssh/motorcade.pem
