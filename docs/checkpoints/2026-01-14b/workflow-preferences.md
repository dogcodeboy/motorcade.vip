# Workflow Preferences (persisted)

- All changes must be **Ansible-managed** (no manual edits/uploads in WP Admin for permanent fixes).
- Do **not overwrite** older checkpoints.
- Every session handoff must include a new FULL checkpoint directory.
- Deliverables must be downloadable ZIPs (not mounted).
- GitKraken is used for commits.
- Theme and content must respect the original mockup.
